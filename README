* ssh -x asdfr_cbl_08@10.0.15.55
* password advanced_decision
* run clone command
* run "g++ signal_nwait.cpp -o signal"
* run "./signal"
* run "htop" to see processor load, take a screenshot of it
* run "cpufreq-info" if it not installed "sudo apt-get install cpufrequtils", take screenshot
* change the file name to "...2.txt"
* run "g++ signal_nwati.cpp -o signal" again
* run "stress --cpu 1k --timeout 60s"
* run "./signal"
* run "cpufreq-info", take screenshot
